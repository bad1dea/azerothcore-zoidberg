# Test Matrix

Tracks what's verified, how, and as of which commit. Update whenever a gate
adds coverage.

## Gate 0 — project foundation

| Check | Method | Status |
|---|---|---|
| Module directory is auto-discovered by CMake | Manual inspection of `GetModuleSourceList` glob behavior against `modules/mod-autonomous-player/src` | Verified (design-time, no build run this session — see `HANDOFF.md`) |
| Module has no Playerbots source dependency | `modules/mod-autonomous-player/tools/check_no_playerbots_dependency.sh` | Automated, run every session |
| Module has no forbidden player-like-policy API usage | `modules/mod-autonomous-player/tools/check_no_forbidden_apis.sh` | Automated, run every session |
| Module compiles standalone (no compiler on this dev box; only real build path is the zoidberg host) | `docker build --target worldserver` on host zoidberg against this branch | See `HANDOFF.md` verification section for this session's result |
| `BotLifecycleMgr` registry add/remove/tick-stagger logic | Manual code inspection this session (see `ARCHITECTURE.md` ADR-001). No gtest coverage yet — this repo's Google Test suite (`src/test/`) is core-only; module-level tests aren't wired into the zoidberg build's `BUILD_TESTING` flag (currently off in `apps/docker/Dockerfile`). Revisit once a Gate makes the arithmetic non-trivial enough to be worth wiring up. | Deferred, documented gap |
| `PerceptionSnapshot` is a value type with no engine pointers | Code review: struct has no `Player*`/`Unit*`/`Creature*` members (see header) | Verified by inspection |

## Gate 1 — first complete quest (first slice: online bot + perception)

| Check | Method | Status |
|---|---|---|
| Bot account provisioning (`.autonomousplayer provision`) | Live on zoidberg: `ap_test1` account created via `AccountMgr::CreateAccount` | Verified |
| Bot character creation via real opcode handler | Live on zoidberg: `Grunttestbot` (Orc/Warrior/male) created via `HandleCharCreateOpcode`, confirmed in `acore_characters.characters` | Verified |
| Bot login (no teleport, correct starting position) | Live on zoidberg: `.autonomousplayer login ap_test1 Grunttestbot` → online at map 1, `(-618.5, -4251.7, 38.7)` (Valley of Trials) | Verified |
| `BotLifecycleMgr` registration on real login | Live: `PLAYERHOOK_ON_LOGIN` → "1 bot(s) now registered" | Verified |
| `PerceptionSnapshot` read path (level/map/position/health/alive) | Live: perception log line matches expected values exactly | Verified |
| Bot session survives across world ticks | Live: `BotSessionMgr`-tracked session ticked repeatedly via `MapSessionFilter`, no premature deletion | Verified |
| No Playerbots dependency / no forbidden APIs | `check_no_playerbots_dependency.sh`, `check_no_forbidden_apis.sh` | Automated, pass every commit |
| Restart-resume (bot automatically comes back online after a worldserver restart with no manual login) | Not yet implemented — `Persistence` component (ADR-004) is still a stub. Currently a bot only comes online via explicit `.autonomousplayer login`. | Deferred, tracked in `ROADMAP.md` backlog |
| `BotLifecycleMgr::IsRegistered` false after logout | Mechanism implemented (`QueueForRemoval` in `OnPlayerLogout`) but not yet live-exercised (no test triggered an actual bot logout this session) | Deferred, low risk — revisit before closing full Gate 1 |

## Gate 2 — levels 1–6 (in progress)

| Check | Method | Status |
|---|---|---|
| `Navigation::MoveTo` uses real pathing, not teleport | Live on zoidberg: bot walked `(-618.5,-4251.7,38.7)` → `(-598.5,-4251.7,39.0)`; Z snapped to real terrain height (39.0), proving navmesh-based movement, not a position copy | Verified |
| `QuestEngine::RequestAcceptQuest` accepts a real quest via real opcode handler | Live on zoidberg: quest 4641 from creature 10176 (Kaltunk), `GetQuestStatus` went `0` → `1` (`QUEST_STATUS_COMPLETE`) after moving into real interaction range | Verified |
| Quest-accept respects real interaction range (not just "nearby") | Live: first attempt at ~8.6 yards silently failed (`QUEST_STATUS_NONE`); succeeded at ~1-2 yards | Verified (documented as expected behavior, not a bug — see `HANDOFF.md`) |
| No Playerbots dependency / no forbidden APIs | `check_no_playerbots_dependency.sh`, `check_no_forbidden_apis.sh` | Automated, pass every commit |
| `QuestEngine::RequestChooseReward` turns in a real quest via real opcode handler | Live on zoidberg: quest 4641 to creature 3143 (Gornek), status `1` (`QUEST_STATUS_COMPLETE`) → `6` (`QUEST_STATUS_REWARDED`), `IsQuestRewarded` true, XP `0` → `40`; confirmed both in-game and in `character_queststatus_rewarded` | Verified |
| Turn-in bypasses `HandleQuestgiverCompleteQuest` (UI-only, no-op for socketless bot) in favor of `HandleQuestgiverChooseRewardOpcode` (real reward grant) | Code review — see ADR-011 | Verified by design |
| `Combat::RequestAttack` starts a real melee engagement via real opcode handler | Live on zoidberg: attacked a Mottled Boar (55/55 hp), bot's `IsValidAttackTarget`/`Attack` ran for real via `HandleAttackSwingOpcode` | Verified |
| Combat completes without stalling (chase fix) | Live: after adding `MotionMaster::MoveChase`, killed 2 Mottled Boars and 2 Scorpid Workers cleanly, bot took 0 damage each time, `combat` returned to `false` after each kill | Verified (bug found + fixed this session, see `KNOWN_FAILURES.md`) |
| `Inventory::LootCorpse` opens/checks/releases loot via real opcode handlers | Live on zoidberg: looted all 4 kills above with no errors; reads `Creature::loot.items`/`.gold` directly rather than parsing our own no-op loot-response packet | Verified |
| Loot respects quest-gating (`QuestRequired` loot-table rows) | Live: `Scorpid Worker Tail` (90% chance) correctly did not drop because the bot's related quest was already turned in — confirmed via `creature_loot_template`, not assumed | Verified (documented as correct behavior, not a bug) |
| No Playerbots dependency / no forbidden APIs | `check_no_playerbots_dependency.sh`, `check_no_forbidden_apis.sh` | Automated, pass every commit |
| `Recovery::RequestReleaseSpirit` releases spirit via real opcode handler | Live on zoidberg: genuine death triggered (multi-pull escalation, see below), `HasPlayerFlag(PLAYER_FLAGS_GHOST)` went false→true | Verified |
| `Recovery::RequestReclaimCorpse` resurrects via real opcode handler | Live: after the real ~30-40s delay, `alive` went false→true, `ghost` true→false, full health, corpse cleared | Verified |
| Release-spirit correctly leaves the ghost in place when no graveyard is registered nearby | Live: death in open wilderness far from any graveyard zone; confirmed via code review this matches `Player::RepopAtGraveyard()`'s own documented fallback | Verified (documented as correct behavior, not a bug) |
| Triggering a genuine death requires real, escalating effort | Live: bot survived 3, 5, and 8-creature deliberate multi-pulls of Scorpid Workers (leveling up to 2 mid-fight); died only on a 4th, mixed 3-creature pull | Verified — meaningful confirmation starting-zone content is safe for legitimate play |
| `PerceptionSnapshot.IsGhost`/`HasCorpse`/`CorpseX/Y/Z` | Live: read back correctly at every stage of the death/recovery cycle above | Verified |
| No Playerbots dependency / no forbidden APIs | `check_no_playerbots_dependency.sh`, `check_no_forbidden_apis.sh` | Automated, pass every commit |
| `Economy::BuyItem`/`RepairAll` submit real vendor requests via real opcode handlers | Live on zoidberg: both submitted cleanly against Huklah (creature 3160) with no crashes/errors | Verified |
| Vendor buy respects real insufficient-funds validation | Live: bot had 0 copper, item cost 63; no money spent, no item received — confirmed as `Player::BuyItemFromVendorSlot`'s real check, not a bypass | Verified (documented as correct behavior, not a bug) |
| `Gossip::RequestGossipHello`/`RequestGossipSelectOption` build/select real menus via real opcode handlers | Live on zoidberg: Frang's menu correctly showed `[0] optionType=5 "I require warrior training."`; selecting it submitted with no errors | Verified |
| `Growth::RequestTrainerList`/`RequestLearnSpell` open/learn via real opcode handlers | Live on zoidberg: found spell 6673 (Battle Shout) as eligible via real `Trainer::CanTeachSpell`, submitted cleanly, no crashes | Verified |
| Trainer spell-learning respects real insufficient-funds validation | Live: spell costs 10 copper, bot had 0 — no state change, confirmed as `Trainer::TeachSpell`'s real check, not a bypass | Verified (documented as correct behavior, not a bug) |
| Second race/class (Human Priest) logs in at correct racial spawn | Live on zoidberg: `Priestestbot` online at `(-8950.0,-132.5,83.5)` map 0 (Northshire Abbey), confirmed against the live world DB `playercreateinfo`, no teleport | Verified |
| Second race/class completes a real quest cycle | Live: quest 783 "A Threat Within" (Deputy Willem, creature 823) accepted → complete → turned in to Marshal McBride (creature 197): XP 50→90, `rewarded=true` | Verified |
| Second race/class completes real kill+loot | Live: Diseased Young Wolf (creature 299) killed via melee (`Combat::RequestAttack`), 0 damage taken, looted cleanly | Verified |
| `Combat::RequestCastSpell` calls the real `Unit::CastSpell` pipeline | Live: cast request against spell 585 correctly rejected (`accepted=false`, no HP change) — consistent with a level-1 Priest having no offensive spell yet, not a defect | Verified (mechanism proven; positive damage-cast test deferred, documented as correct behavior) |
| `.autonomousplayer spellbook` reads live in-memory spellbook, not stale DB state | Live: `character_spell` in the DB was empty for a freshly-created, never-saved bot; the new command correctly listed all 42 live spell IDs from `Player::GetSpellMap()` | Verified |
| Every race completing its starting area | 2 of 10 races exercised (Orc, Human) — user confirmed (2026-07-01) this representative sample satisfies Gate 2's bar | Verified (Gate 2 marked complete in `ROADMAP.md`) |
| Second class controller | Human Priest exercised (melee + attempted spell-cast); Warrior and Priest both proven | Verified |

## Gate 3 — levels 1–12 (in progress)

| Check | Method | Status |
|---|---|---|
| `GuideRuntime::Tick` advances a bot through multiple steps automatically, no manual command between them | Live on zoidberg: issued `.autonomousplayer guidestart` once, then only polled `guidestatus`/`status` — `CurrentStep` advanced 0→1→2→3 (`finished=true`) on its own, final position exactly matched the last of 3 waypoints | Verified |
| `BotLifecycleMgr::Update`'s per-bot dispatch actually calls `GuideRuntime::Tick` (first time this call path has ever mattered) | Live: confirmed via the same test above — position changes prove `Navigation::MoveTo` was actually invoked automatically, not just bookkeeping | Verified |
| No Playerbots dependency / no forbidden APIs | `check_no_playerbots_dependency.sh`, `check_no_forbidden_apis.sh` | Automated, pass every commit |
| Combat-capable guide step (`StepType::KillNearest`: walk+kill+loot, fully automatic) against a nearby (~70yd), reachable target | Live on zoidberg: `guidestartcombat` against Mottled Boar (creature 3098) — finished within 15s, boar confirmed dead (`hp 0/55`) via `creaturestatus`, bot took 0 damage, zero manual commands after the single trigger | Verified |
| `KillNearest` against a target found near/past the search radius edge, after 3 fix attempts | Live on zoidberg: attempts 1-2 (arrival-gate, then bare `MoveChase`) each reproduced the identical stall on re-test — both disproven, not just insufficient. Attempt 3 (revert to real `Combat::RequestAttack` + `IsInCombat()` confirmation) verified clean **twice independently**, two different Mottled Boars, both dead within 15s, zero damage taken, zero contamination | **Resolved** — see `KNOWN_FAILURES.md` #3 for the full attempt-by-attempt evidence trail |
| `AcceptQuest`/`TurnInQuest` guide steps: interaction-range gating, already-active-quest fast path | Live: `AcceptQuest` step correctly advanced instantly when the target quest was already active/incomplete in the bot's log (`GetQuestStatus != NONE`) | Verified |
| Full automatic quest loop (AcceptQuest→KillNearest→TurnInQuest, one guide, zero manual steps) | Live: `guidestartquest` for quest 788 advanced 0→1→2 (accept, then a real automatic combat engagement — `combat=true`, took real damage, killed and looted the boar — then into turn-in), stalled correctly (not falsely) at step 2 since the quest genuinely needs 8 kills and only partial progress was made in this run | Verified — real automatic progress through all 3 step types in one guide, including a real mid-chain combat engagement |
| Explicit `PullState` machine (`Selecting`/`Approaching`/`Engaged`/`Looting`) for `KillNearest`, per `HONORBUDDY_SINGULAR_COMBAT_RESEARCH.md` (ADR-022/023) | Live on zoidberg, twice independently: two Mottled Boars, 6s and ~11s completions, no regression from the prior flat-phase version; one run's diagnostics caught mid-flight showed real state progression (`pullState=2` `Engaged`, `approachTicks=5`) | Verified |
| Bounded stuck-timeout + blacklist for a target that never confirms engagement | Implemented (`MaxApproachTicks=20`, `FindNearestNonBlacklisted`) | Not yet exercised by a real unreachable-target scenario live — happy-path runs never needed it (`blacklisted=0` both times), so this specific code path remains unverified, noted honestly rather than assumed |
| `EncounterModel::BuildSnapshot` correctly reads real attacker state (`Unit::getAttackers()`) | Live on zoidberg: idle baseline correct (0 attackers); mid-fight against a real `KillNearest` pull showed `attackers=1`, correct `entry`, correct `isObjectiveTarget=true`, `distance≈0`; standalone `multipull` test showed a correctly-tagged real attacker (`hasUnplannedAdd=true` when no guide objective was set) | Verified (single-attacker case); multi-simultaneous-attacker case not empirically captured (combat resolved faster than polling), honestly noted as unproven rather than assumed |
| `KillNearest` engagement confirmation fixed (`GetVictim()==target`, not `IsInCombat()`) — real bug found via external review | Fixed by reasoning + code review; happy path re-verified correctly live (real target, correct guid, real melee range), 4 separate attempts across multiple pull sizes (2/4/6/8 boars) to force the negative case all failed to sustain overlap — test creatures die faster than console-command latency, a confirmed environment limitation, not further retried | Fixed, happy path re-verified; **the specific scenario the fix targets not proven by direct live observation after 4 honest attempts** — see `KNOWN_FAILURES.md` #5 |
| `EncounterModel` gates `KillNearest`'s engagement confirmation (first real behavior consumer, ADR-027) | Compiles clean, no regression across 4 live test attempts (happy path reconfirmed each time). The gated logic's behavior under a genuine add was not directly observed (same overlap-timing limitation as above) — correct by code review (synchronous check, no window for the review's original bug to reappear), not yet proven by observation | Implemented, no regression; specific gating behavior not directly observed live |
| Full multi-kill quest with real credit and real turn-in, end to end | Live on zoidberg: quest 788 "Cutting Teeth" (8 Mottled Boars) reached genuine `QUEST_STATUS_COMPLETE` after accumulated kills, then `guidestartquest` ran the full accept→kill→turn-in chain and confirmed `status=6` (`QUEST_STATUS_REWARDED`), real XP granted | Verified — a real, complete, automatic quest cycle, not just step composition |
| Bounded failure states for every previously-unbounded guide wait (ADR-028), directly responding to external review priority 3 | Live on zoidberg: happy-path `guidestartcombat` completed cleanly (`finished=true, failed=false`) well under the bound, no regression. New `.autonomousplayer guidestartmoveto` command targeted a genuinely unreachable coordinate `(5000,5000,500)` — real tick progression observed (`operationTicks=23` at +10s, `=46` at +20s), timeout genuinely fired at the bound (`finished=true, failed=true`), bot's state confirmed sane afterward (real partial navmesh path walked, fully controllable via a follow-up command), no crashes | Verified — real timeout observed firing, not just code review. Found and corrected an inaccurate comment (real tick rate ~2.3/s, not the assumed ~1/s — bound is safe either way, just faster than documented) |
| Warrior real offensive ability (spell 78) beyond bare melee, using real `SpellCastResult` diagnostics | Live on zoidberg, twice independently: cast rejected early in combat (low rage), accepted (`result=255 SPELL_CAST_OK`) once real rage had accumulated (179, then 234 on a second attempt) from a few seconds of auto-attack, target confirmed dead shortly after each successful cast | Verified — real root cause confirmed (insufficient rage, not a defect), `Combat::RequestCastSpell` needs no changes, real damage-dealing cast beyond melee demonstrated for the first time this arc |
| First class-controller slice: `KillNearest`'s `Engaged` phase opportunistically tries a real ability (ADR-029) | Live on zoidberg, originally 3 runs (2/3 clean, 1/3 timed out in `Engaged`). **Follow-up session, 13 further valid trials** (careful to poll for completion before starting the next, after one trial was invalidated by starting too early): **12/13 clean, 1/13 failed but in `Selecting` (target out of range), not `Engaged`** — the original `Engaged`-timeout pattern did not recur at all across the larger sample | **Re-tested, not reproduced** — good evidence (not proof) the original 1/3 was an unlucky individual creature, not a systemic `RequestCastSpell`/melee-timing interaction; see `KNOWN_FAILURES.md` #6 |
| Real loot success verification (`LastLootAttempted`/`LastLootVerified`, ADR-030) | Live on zoidberg, 3 independent runs against real Mottled Boar kills: `lastLootAttempted=true, lastLootVerified=true` every time, no crashes | Verified — consistent, clean, closes the explicitly-deferred ADR-028 gap |
| Target selection safety, `attackable` check (`IsSafeToEngage`, ADR-031) | Live on zoidberg: `.autonomousplayer targetsafety Grunttestbot 3098 300` against a real Mottled Boar → `attackable=true, safe=true`. **Caught a real regression before deploy**: the first version used `IsHostileTo` and reported `hostile=false` for the same boar (most questing wildlife is faction-neutral, not Hostile) — would have permanently rejected the project's most-tested target. Fixed to `IsValidAttackTarget`, re-verified | Verified — regression caught and fixed by the diagnostic itself, not just re-confirmed |
| Target selection safety, no regression in `KillNearest` (ADR-031) | Live on zoidberg: `guidestartcombat` with `IsSafeToEngage` wired into selection + `Approaching` re-check completed identically to every pre-ADR-031 run — `pullState` progressed Selecting→Approaching→Engaged (`hasUnplannedAdd=false`) then `finished=true, failed=false, lastLootVerified=true` | Verified |
| Character-name pre-validation (ADR-032), fixes `KNOWN_FAILURES.md` #9 | Live: re-submitting the over-length name that stalled before now fails in <1s with the real reason ("too long"); a validly-named second character (`Grunttestii`) created successfully on the first attempt | Verified — root cause found and fixed, not worked around |
| Target selection safety, tap/other-player-attacking checks (ADR-031) | Live, two real bots (`Grunttestbot`, `Grunttestii`) colocated near a boar-dense spot: `Grunttestii` set attacking a specific boar, `targetsafety` on that same boar from `Grunttestbot` → `hasLootRecipient=true tappedByBot=false otherPlayerAttacking=true safe=false`. Then a full `guidestartcombat` run confirmed `KillNearest` itself (not just the diagnostic) picks a *different* boar and completes cleanly while the tapped one is independently killed by the other bot | Verified — both the diagnostic and the composed `KillNearest` behavior |
| Target selection safety, LoS check (ADR-031) | Live, found opportunistically near Razor Hill: `targetsafety` on a penned Swine (entry 10685) returned `los=false → safe=false` while four other nearby entries checked at the same time all returned `los=true` — reproduced on a second immediate poll | Verified |
| Target selection safety, evade check (ADR-031) | Code review only (standard, unmodified engine API). Live attempts (pull a boar, flee to break chase) were made too, but Mottled Boars die/reset faster than console-command round-trip latency allows a genuine mid-evade state to be caught — same environment limitation as `KNOWN_FAILURES.md` #5 | Not verified live — same calibration as `KNOWN_FAILURES.md` #5 |
| Automated regression suite, `tools/live_regression_suite.py` (ADR-033, review priority 7) | Live: full run against `Grunttestii`, entry 3098 → `5/5 passed`, including a direct regression test for ADR-031's `IsHostileTo`→`IsValidAttackTarget` fix. The suite's own first run caught 2 real bugs in itself (comma-terminated key=value parsing, too-tight timeout) — fixed and re-verified | Verified — first automated regression protection this whole project has had |
| Bounded-timeout guides stop movement, not just bookkeeping (ADR-035, fixes `KNOWN_FAILURES.md` #10) | Live: repeated the exact `guidestartmoveto` scenario that previously killed a bot — guide still hits `failed=true` at the bound, but position now static across 5 polls (~30s) afterward instead of continuing to drift, bot survives (`alive=true` throughout). `live_regression_suite.py` still `5/5` after the fix | Verified — real safety gap closed, re-tested with the exact prior failure scenario |
| Third class archetype: Hunter, ranged opportunistic ability (ADR-034) | Live: `Grunthunter` (Orc Hunter) vs. real Mottled Boar, `guidestartcombatability ... 75` (Auto Shot) → `finished=true, failed=false, lastLootVerified=true` in ~15s, same `KillNearest`/`OpportunisticSpellId` composition as the Warrior slice, no code changes needed | Verified — first ranged-class combat coverage this project has had |
| Pets: tame, status, react state (ADR-037) | Live: `tamebeast` → real Tame Beast cast → real `character_pet` row; `petstatus` correctly reports pet state before/after taming; pet **persists across a full worldserver restart+relogin** (real engine behavior); `petreactstate ... 2` (aggressive) confirmed via `petstatus`; `guidestartcombat` completes cleanly with the pet present, no regression | Verified — real primitives + diagnostics, first pets coverage this project has had |
| Pets: does an aggressive pet actually assist in combat | Live: added `PetSnapshot::VictimGuid`; during a real `guidestartcombat` fight the pet's `victim` matched the bot's own objective target's exact guid while `Engaged`, then reset to `none` once it died | Verified — direct, observed proof, not an inference |
| `GuideRuntime` pet integration: `EnsurePetAssists` keeps the pet defensive + on the guide's own target (ADR-038) | Live: `reactState` flipped from aggressive(2) to defensive(1) automatically within the first tick; `petstatus`'s `victim` matched the guide's exact objective-target guid during `Approaching`/`Engaged`; guide completed cleanly (`finished=true, failed=false, lastLootVerified=true`); `live_regression_suite.py` still `5/5` | Verified — real, observed, no regression |
| `CombatIntent`/`CombatExecutor` refactor, no regression (ADR-039) | Live: `EngageTarget`/`UseAbility`/`AssistPetOnTarget` all routed through the shared `Combat::Execute` -- `guidestartcombat` still completes cleanly, `tools/live_regression_suite.py` still passes | Verified — no regression from the refactor |
| Pet recovery: guide runs normally with no pet at all | Live: `guidestartcombat` completed cleanly with `PetState::NoPet`, no incorrect stall | Verified |
| Pet recovery: `RequestRevivePet` actually revives a `PetState::MissingDead` pet (ADR-040) | Live, twice: cast against `Grunthunter`'s real broken pet (`character_pet.slot=100, curhealth=0`), result `SPELL_CAST_OK`, `petstatus` showed the same pet number load back in alive and regenerate normally | Verified — real, reproduced, matching pet identity confirmed |
| `PetState` 6-state taxonomy (`NoPet`/`ActiveAlive`/`ActiveDead`/`MissingAlive`/`MissingDead`/`Dismissed`) reads real `PetStable` health, not just presence (ADR-040) | Live: `ClassifyPetState` correctly resolved `MissingDead` for `Grunthunter`'s broken pet by reading `PetStable::GetUnslottedHunterPet()->Health == 0` | Verified |
| `RequestCallPet` (`PetState::MissingAlive` recovery) | Superseded — later verified live end to end, see the ADR-043 row below | Verified (ADR-043) |
| Pet revival persistence across an abrupt (non-clean) worldserver restart | Live, negative: a revived pet's DB row still showed the pre-revive dead/unslotted state after an abrupt restart shortly after revival (no explicit `SavePetToDB` in the null-pet revival branch) | Verified as a real, expected characteristic (same as other engine transient state) — not re-tested across a clean logout or the normal 900s autosave interval, low value for this session's time budget |
| Pet recovery does NOT preempt an active `KillNearest`/quest pursuit (`CurrentTargetGuid.IsEmpty()` gate, ADR-040 fix) | Live: `live_regression_suite.py` went from intermittently exceeding its 40s wall-clock timeout (recovery starving `OperationTimedOut`'s tick counter during `Approaching`/`Engaged`) back to a clean `5/5` after the gate; later, live-confirmed directly — `petstatus` showed `MissingDead` while the guide was genuinely `Engaged` with a live target, and recovery correctly did not fire | Verified — real, reproduced (regression before/after), and directly observed holding off during an active pursuit |
| Pet recovery does not interrupt its own in-flight cast (`Unit::IsNonMeleeSpellCast` gate, ADR-040 fix) | Reasoned from real engine behavior (`Unit::CastSpell` restarts an already-in-progress cast, same as a player re-clicking a spell) plus the regression suite returning to `5/5` after adding the gate | Verified via regression outcome — the specific "does a revive cast now survive a full cast time under repeated eligible ticks" scenario not isolated in a standalone test |
| Residual intermittent `guidestartcombat` wall-clock timeout, later in the same session | Live: guide stuck in `Engaged` with pet confirmed `MissingDead` and `CurrentTargetGuid` non-empty (recovery correctly not firing); guide still finished via its own `MaxOperationTicks` bound (`Failed=true`), just slower than the 40s test window | Investigated, attributed to pre-existing `KNOWN_FAILURES.md` #6 (ADR-029 Engaged-phase timeout), not pet recovery — small sample, not chased further |
| Ranged pulls as a distinct behavior (not just a ranged spell used at melee range), Warlock pets | Not attempted — no infrastructure | Deferred, real scope gap, `HANDOFF.md` `NEXT TASK` |
| Dense camps/caves, broader guide validation, remaining 8 races | Not yet attempted | Deferred — same representative-sample interpretation as Gate 2 (`ROADMAP.md`), `HANDOFF.md` `NEXT TASK` |
| Full bags | Encountered organically this session (`Grunttestbot`, 18 inventory items after 15+ kills, produced a real `lastLootVerified=false`) — not a deliberately engineered test, but real evidence the existing best-effort loot design (ADR-030) behaves correctly under it | Partially covered, organically |
| `creaturestatus`'s `FindNearestCreature` alive-param fix (`KNOWN_FAILURES.md` #8) | Live: found a real, live, nearby creature (`Spirit Healer`, entry 6491, `alive=true`, confirmed via SQL distance query) that the unfixed alive=false-only search would have missed | Verified — real bug (hit multiple times this session's own testing), real fix, applied to both known call sites now |
| User-reported `Grunthunter` underground/Z-clipping, suspected `MovePoint`/`forceDestination` mechanism (`KNOWN_FAILURES.md` #14) | Live: deliberately reproduced the suspected mechanism twice (the regression suite's own unreachable-target walk), watching `Z` continuously through both ~20s walks — both showed smooth, terrain-tracking `Z` throughout, no clipping | Investigated, NOT reproduced via the suspected mechanism — real root cause still unknown, honestly flagged open, not claimed fixed |
| `live_regression_suite.py` position-restore after the unreachable-target test (`KNOWN_FAILURES.md` #14 hardening) | Live: bot's position before vs. after the full test cycle matched (within float rounding) — confirmed the restore-walk-back logic works | Verified |
| `Pets::FindNearestTameableBeast` (real grid search, ADR-041) | Live: correctly found a real, live, nearby Mottled Boar and matched it against `CreatureTemplate::IsTameable` | Verified — used successfully in both manual and automatic taming tests below |
| Auto-tame-if-no-pet: manual `tamebeast` on a `HasSpell`-lacking Hunter (surfaced `KNOWN_FAILURES.md` #15) | Live: `.autonomousplayer tamebeast` against a level-1 Hunter with no spell 1515 in her spellbook still returned `SPELL_CAST_OK` and produced a real, alive pet | Verified — real, surfaced the `HasSpell`-was-wrong-gate bug before it shipped |
| Auto-tame-if-no-pet: `Recovery::PlanPetAcquisition` fires fully automatically, no manual command (ADR-041) | Live: a fresh Hunter (`PetState::NoPet` confirmed) walked near a real beast and left idle — no `tamebeast` call issued — ended up with a real, live, newly-tamed pet moments later, confirmed via `petstatus` polling alone | Verified — first fully-automatic (not debug-command-triggered) pet-acquisition confirmation in this project |
| Revive Pet (982) / Call Pet (883) real regardless of `HasSpell` (surfaced `KNOWN_FAILURES.md` #16) | Live: `.autonomousplayer castspell` with both ids against a Hunter whose spellbook lists neither returned the real `SPELL_FAILED_ALREADY_HAVE_SUMMON` (not unknown-spell) while she had an active pet | Verified — same bug class as Tame Beast, confirmed for both remaining Hunter pet spells |
| `Recovery::PlanPetRecovery`'s `RecoverPet` fires fully automatically after the `HasSpell`→`IsClass` fix (`KNOWN_FAILURES.md` #16, corrects an earlier over-claim in ADR-040) | Live: `Grunthunter`'s pet (`PetState::MissingDead`, unchanged from earlier this session) came back alive (same pet number, 5914) with a guide actively running and **no manual `revivepet` call** | Verified for real this time — the earlier ADR-040 "fully verified live" claim had only tested the raw primitive via the manual debug command, never the actual policy gate |
| `GuideRuntime::Tick` only fires while a guide is actively running (`!BotGuideState.Finished`) | Live, discovered while testing the above: an idle `Grunthunter` with no guide running showed zero pet-recovery activity across multiple `petstatus` polls; starting even a trivial `guidestartmoveto` (to his own current position) made recovery fire within seconds | Verified — real architectural characteristic, not a bug: pet recovery/acquisition has no ambient path independent of guide activity |
| `RequestCallPet`'s `PetState::MissingAlive` -> `Alive` transition (ADR-043, closes `KNOWN_FAILURES.md` #17) | Live, directly observed with 40ms polling: `dismisspet` (real Dismiss Pet spell 2641, `Spell::EffectDismissPet` -> `PET_SAVE_NOT_IN_SLOT`) returned `SPELL_CAST_OK`, real ~5s cast time, then the exact transient `state=MissingAlive` was captured, then `Recovery::PlanPetRecovery`'s `CallPet` intent fired on the next ambient tick and the same pet number (5988) came back alive within 0.6s, zero manual recovery commands | Verified — direct observation of every state in the chain; first live confirmation of Call Pet spell 883 itself; regression suite `5/5` before and after |
| `GuideRuntime::TickAmbient` fires pet recovery/acquisition for a fully idle bot with ZERO guide commands (ADR-042, closes `KNOWN_FAILURES.md` #16's ambient-maintenance gap) | Live: `Petulantia` (`PetState::NoPet`) was logged in near her spawn with no `guidestartmoveto`/`tamebeast`/any command issued — got a real, live, auto-tamed pet within seconds of the next `petstatus` poll | Verified — the exact scenario that was impossible before this fix (no guide meant `Tick()` never ran at all) |
| `TickAmbient`/`Tick` split doesn't let a same-tick guide-step dispatch interrupt an ambient-issued cast (ADR-042 self-review correction) | Not live-reproduced (the specific race window is narrow and neither of this session's live tests happened to hit it) — caught by re-reading the change, fixed by making `TickAmbient` return `bool` and having `BotLifecycleMgr::Update` skip `Tick()` for that fire when true, restoring the original pause-by-skipping semantics explicitly | Fixed via code review, not live reproduction of the bug itself; regression suite re-run clean after the fix |
| A dead bot's `guidestartmoveto` guide gets permanently, unboundedly stuck via `TickAmbient` re-attempting a doomed instant-fail cast every tick (`KNOWN_FAILURES.md` #19) | Live, real: `Huntonia` (real death, pet `MissingDead`) showed `operationTicks` frozen at 44 across two `guidestatus` polls with zero change — a genuine hang, not a slow run | Confirmed live as a real bug; the `IsAlive()` fix itself re-verified only indirectly (bot was alive again post-fix, so a fresh guide completing normally is the confirmation, not a direct re-reproduction of the hang) |
| `BotSession::ConsecutiveAmbientSkips` systemic backstop doesn't break normal operation | Live: `Huntonia`'s pet auto-revived via `TickAmbient` on login, then a fresh `guidestartmoveto` completed normally (`finished=true, failed=false`) with no hang or unexpected delay | Verified — the safety net is a no-op under normal conditions |
